name: Checkout Example

on: workflow_dispatch

jobs:
  checkout-repository:
    runs-on: ubuntu-22.04  # ✅ Use updated version
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: List files in repo
        run: ls -la $GITHUB_WORKSPACE
